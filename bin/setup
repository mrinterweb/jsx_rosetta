#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t'
set -vx

bundle install

# Install the Node sidecar's npm dependencies so the JSX parser can run.
script_dir="$(cd "$(dirname "$0")" && pwd)"
(cd "$script_dir/../node" && npm install)
